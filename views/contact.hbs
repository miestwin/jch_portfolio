<style>
.contact {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    color: #ffffff;
}

.contact__header {
    display: flex;
    margin-bottom: 30px;
    text-align: center;
    font-size: 18px;
    letter-spacing: 0.5px;
}

.contact__header a {
    color: #999;
}

.contact__header a:hover {
    text-decoration: underline;
    color: #ffffff;
}

.contact__form {
    width: 400px;
    max-width: 100%;
}

.contact__form label {
    display: block;
    margin-bottom: 10px;
}

.contact__form input,
.contact__form textarea {
    background-color: transparent;
    border: 3px solid #fff;
    outline: none;
    padding: 7px;
    color: #ffffff;
    font-size: 18px;
    width: 100%;
}

.contact__form textarea {
    height: 150px;
}

.contact__form button {
    width: 100%;
    color: #000000;
    padding: 10px;
    outline: none;
    border: none;
    cursor: pointer;
    background-color: #ffffff;
    font-weight: 700;
    letter-spacing: 3px;
    text-transform: uppercase;
}

.contact__form__group {
    margin-bottom: 20px;
}
</style>
<section class="contact">
    <header class="contact__header">
        <p>Wyślij mi wiadomość e-mail bezpośrednio na adres <a href="mailto:jakub.chelstowski12@wp.pl">jakub.chelstowski12@wp.pl</a> lub skorzystaj z formularza.</p>
    </header>
    <form action method="POST" class="contact__form">
        <div class="contact__form__group">
            <label for="name">Imię i Nazwisko</label>
            <input type="text" id="name" name="name" required>
        </div>
        <div class="contact__form__group">
            <label for="email">E-mail</label>
            <input type="email" id="email" name="email" required>
        </div>
        <div class="contact__form__group">
            <label for="message">Wiadomość</label>
            <textarea id="message" name="message" required></textarea>
        </div>
        <button type="submit" class="contact__form__submit">
            Wyślij
            <span class="l-icon l-spinner spinner"></span>
            {{#if success}}
            <span class="l-icon l-checkmark text-success"></span>
            {{/if}}
            {{#if error}}
            <span class="l-icon l-cross text-danger"></span>
            {{/if}}
        </button>
    </form>
    <script>
        var spinner = document.querySelector('.contact__form__submit .spinner');
        var contactForm = document.querySelector('.contact__form');
        contactForm.addEventListener('submit', function () {
            spinner.classList.add('visible');
        });
    </script>
</section>